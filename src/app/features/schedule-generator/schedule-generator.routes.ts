import { Routes } from '@angular/router';
import { ScheduleGeneratorComponent } from './schedule-generator.component';

export const SCHEDULE_GENERATOR_ROUTES: Routes = [
  { path: '', component: ScheduleGeneratorComponent }
];
